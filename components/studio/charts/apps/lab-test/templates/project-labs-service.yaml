apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-labs-svc
  namespace: {{ .Values.namespace }}
  labels:
    run: {{ .Release.Name }}-labs-svc
spec:
  ports:
  - port: 80
    targetPort: 8888
    protocol: TCP
  selector:
    release: {{ .Release.Name }}