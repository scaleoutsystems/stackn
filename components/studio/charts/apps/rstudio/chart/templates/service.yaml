apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-rstudio
  namespace: {{ .Values.namespace }}
  labels:
    run: {{ .Release.Name }}
spec:
  ports:
  - port: 80
    targetPort: 8787
    protocol: TCP
  selector:
    release: {{ $.Release.Name }}