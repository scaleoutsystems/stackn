apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name | replace "_" "-" }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    release: {{ .Release.Name }}
