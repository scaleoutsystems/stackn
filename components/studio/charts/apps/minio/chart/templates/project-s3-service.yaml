apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-minio
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - protocol: TCP
    port: 9001
    targetPort: 9001
  selector:
    release: {{ .Release.Name }}
