apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-pred-svc
  namespace: {{ .Values.namespace }}
  labels:
    run: {{ .Release.Name }}-pred-svc
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    release: {{ .Release.Name }}
    pod: studio